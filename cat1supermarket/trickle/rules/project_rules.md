When building this project:
- Maintain a Multi-Page Application (MPA) structure.
- `index.html` is for Authentication (Login/Register).
- `dashboard.html` is for the main application logic (Customer/Admin views).
- Use `utils/db.js` for all database interactions to ensure consistency.